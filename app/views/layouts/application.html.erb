<!DOCTYPE html>
<html>
  <head>
    <title>Pizzeria</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous"> %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500&family=Poppins:ital,wght@1,300&display=swap" rel="stylesheet">
    <style>
        .error { color: red; }
        .success {color: green;}
    </style>
    <div class="header">
      <h1>Personal Pan Delights</h1>
      <p>A pizzera with you in mind.</p>
    </div>

  </head>
    <nav class="nav-bar">
      
      <%= link_to 'Home', root_path %>  <%= link_to 'Pizzas', pizzas_path %>
      <% if logged_in?%>
        <%= link_to 'Order Now',  new_order_path %> 
        <%= link_to 'Account', user_path(current_user) %> 
        <%= link_to 'Orders', user_orders_path(current_user) %> 
        <%= link_to 'Logout', logout_path, method: :delete %>
      <% else %>
        <%= link_to 'Sign Up', signup_path %> 
        <%= link_to 'Login', login_path %>
      <% end %>
    </nav>  
  <body>

    
  <% if flash[:error] %>
    <section class='error'><%= flash[:error]%></section>
  <% else %>
    <section class='success'><%= flash[:success]%></section>
  <% end %>
    <%= yield %>

 
  </body>

</html>
